{
  "name": "repo-blueprint",
  "displayName": "Repo Blueprint",
  "description": "Analyzes your repository and displays structural statistics in a side panel — line counts, function detection, import analysis, and more.",
  "publisher": "GreatDeveloper",
  "version": "1.0.1",
  "license": "MIT",
  "keywords": [
    "repository analysis",
    "code statistics",
    "diagram",
    "readme generator",
    "repo structure"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/GreatDeveloper66/repo-blueprint"
  },
  "engines": {
    "vscode": "^1.118.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "repo-blueprint.showStats",
        "title": "Show File Stats"
      }
    ],
    "keybindings": [
      {
        "command": "repo-blueprint.showStats",
        "key": "ctrl+shift+f",
        "mac": "cmd+shift+f",
        "when": "editorIsOpen"
      }
    ]
  },
  "icon": "images/smaller-repo-blueprint.png",
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.19.17",
    "@types/vscode": "^1.118.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.39.3",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1",
    "webpack": "^5.105.3",
    "webpack-cli": "^6.0.1"
  }
}